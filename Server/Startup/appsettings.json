{
  "AppOptions": {
    "JwtSecret": "${JWT_SECRET}",
    "DbConnectionString": "${DOCKER_CONN_STR}",
    "REST_PORT": "${REST_PORT}",
    "WS_PORT": "${WS_PORT}",
    "PORT": "${PORT}",
    "Seed": "${SEED}"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}